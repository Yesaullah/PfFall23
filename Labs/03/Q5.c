#include <stdio.h>
#include <math.h>

main() {
	int distance;
	distance=10+10+5+15;
	int fuel_cost;
	fuel_cost= distance * 2;
	printf("The total cost for fuel used is");
	printf("%d", fuel_cost);
	return 1;
}
