#include <stdio.h>
#include <math.h>

int main() {
  int volume;
  int width;
  int height;
  int length;
  length=10;
  width=10;
  height=20;
  volume=length*width*height;
  printf("%d",volume);
  return 1;
}
